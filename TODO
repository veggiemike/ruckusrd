-*- mode: outline -*-

* add a squashfs= boot arg to specify an image

* fix all the FIXMEs

* test with DM udev events turned on, document results

* migrate to new versions of udev and systemd (and update README)
